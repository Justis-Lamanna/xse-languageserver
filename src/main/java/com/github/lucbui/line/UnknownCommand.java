package com.github.lucbui.line;

public class UnknownCommand {
    private CommandLine command;

    public UnknownCommand(CommandLine command) {
        this.command = command;
    }

    public CommandLine getCommand() {
        return command;
    }
}
